# Copier vers .env.local pour brancher le frontend local sur l'API VPS
# cp .env.vps.example .env.local
#
# Le frontend (localhost:3000) enverra les requêtes /api vers le VPS

VITE_API_TARGET=http://91.99.61.232:3000
